{
  "buildCommand": "cd frontend && npm install && npm run build && node ../copy-dist.cjs",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}